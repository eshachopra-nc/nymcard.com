# CLAUDE.md

This document is read by Claude Code (the CLI agent that writes code in this repository) before any code is generated, before any file is edited, and before any pull request is opened. It is the gatekeeper between design intent and implementation.

Treat this document the same way you treat compile errors: if you would do something that contradicts it, stop and ask.

---

## Read order on every session

When you (Claude Code) start a session in this repository, read these documents in order, before doing anything else:

1. **This file (`CLAUDE.md`)** — rules of engagement.
2. **`../01-design-system/design-system.md`** — what the visual outcome must be. Authority over visual decisions.
3. **`../01-design-system/tokens.json`** — machine-readable token values. Authority over specific values. Mirrored at build time into `./lib/tokens.ts` (typed TypeScript constants for non-Tailwind contexts: Framer Motion, inline styles, SVG attributes) and into the `@theme` block in `app/globals.css` (Tailwind utility classes); both are generated by `scripts/build-tokens.js` — never hand-edit them.
4. **`../00-strategy/stack-decision.md`** — which libraries do which jobs. Authority over technical choices.
5. **The relevant copy file in `../02-copy/`** — the locked copy for the page you're working on (e.g. `../02-copy/Homepage.md`). Authority over text content.

You do not need to re-read these mid-session unless they have been updated. You do need to read them at the start of every session.

---

## Design guardrails — anchor to the visual system (EVERY agent, non-negotiable)

These exist because work kept drifting flat: agents reinvented surfaces instead of leveraging the visual system, so the hero looked premium and everything else looked generic. Every design/build agent (ui-ux-designer, product-ui-designer, marketing-page-builder, qa-engineer) and Claude Code obey these. If a task would break one, stop and ask.

1. **Compose from the visual system; never reinvent a surface.** The hero and the `/visual-system` styleguide are the standard. Build every section by composing existing canonical components/primitives — do not hand-author a new card, panel, glass, or atmosphere when one exists.
2. **The canonical kit (use these, don't replace them):**
   - **Glass = `components/visuals/GlassPanel.tsx`** (design-system.md §8.1). The one frosted-glass material.
   - **Glass MUST float on a rich field, NEVER a solid/near-plain bed** (§8.1, verbatim: "never place glass over a solid colour — it reads over imagery or gradients"). Place `GlassPanel` over **`components/visuals/GlassAtmosphere.tsx`** or the kinetic ribbon — exactly what the hero card floats on. Frost on a faint wash collapses to a flat rectangle; that was the bento bug.
   - **Reference + bar: `/visual-system/glass`.** Its before/after makes the rule undeniable. Match it; verify against it.
   - Tokens only (`lib/tokens.ts` + Tailwind utilities + visual-engine `visual`/`withAlpha`); cool palette only; no raw hex/px.
   - **RESTRAINT — light-first, never garish.** The atmosphere is theme-aware (soft pastel field in light, deep cool in dark) and is CONTAINED to the surface's own card — never a full-section colour wash. The page stays light-first and restrained (design-system.md §1); cool, but never a saturated rainbow. If a section looks colourful at full-page scale, it's wrong.
3. **Motion model (uniform):** content is static at rest; the field may drift ambiently (hero-consistent); each surface reveals on scroll-into-view (`whileInView`, once) and has a hover signature (`group-hover`). Framer Motion only; every motion `prefers-reduced-motion` safe. No perpetual content animation, no AI-slop motion (pulsing dots, uniform fades, fake live tickers).
4. **If the kit is missing something, STOP and propose adding it to the kit** (with a one-line rationale) — never ship a one-off. New primitives are folded back into `design-system.md` + `/visual-system` so the system stays one source of truth.
5. **Verify before "done":** render light AND dark, drive hover/scroll (or audit the classes), check against the hero standard + `/visual-system`. A static, flat, or single-theme result is not done. qa-engineer gates this before any human review.

## What this repository is and is not

This repository is the **build** for nymcard.com. Specifically: it converts approved designs into Next.js code, faithfully and without visual invention. Design happens upstream (in Claude Design and Stitch, captured as static artefacts in `../05-handoff/`). Copy happens upstream and is captured as Markdown files in `../02-copy/`. Your job is implementation fidelity, not creative direction.

You are not:
- Designing the visuals. The visuals come from `../05-handoff/`.
- Writing the copy. The copy lives in `../02-copy/` (the files listed in the locked copy section below).
- Picking libraries. The stack is in `stack-decision.md`.
- Inventing tokens. The tokens are in `tokens.json`.

You are:
- Translating approved designs into clean, accessible, performant React + Tailwind code.
- Wiring up motion using Framer Motion against the motion tokens.
- Mirroring copy verbatim from `../02-copy/`, never inventing or improvising it inline.
- Asking clarifying questions when handoff artefacts are ambiguous.

---

## Hard rules

These are the rules where drift caused the previous build to fail. They are non-negotiable. If a request would require breaking one, stop and ask first.

### Rule 1 — Never invent visual decisions.

If the handoff artefact in `../05-handoff/` does not specify a value (colour, spacing, radius, motion timing, layout), the answer is one of:

1. The token in `tokens.json`.
2. A clarifying question to the user.

It is never your own guess. "Looks about right" is not acceptable.

### Rule 2 — Never invent copy.

All marketing copy comes from the Markdown files in `../02-copy/` — that folder is the source of truth (e.g. `Homepage.md`, `Navigation.md`). Mirror the copy verbatim into the component; a typed `COPY` const at the top of the section component is the established pattern (see `components/hero/Hero.tsx`). Placeholder copy is acceptable for component-level work that is not section-specific; if you use placeholder copy, mark it clearly with a `// TODO: copy from ../02-copy/` comment.

When you mirror copy, copy it exactly as written. Do not edit, tighten, or "improve" the copy. The copy is approved at a different layer. If `../02-copy/` and a component ever disagree, `../02-copy/` wins — update the component, not the file.

### Rule 3 — Use only the libraries in `stack-decision.md`.

The stack is locked. Adding a new library is a deliberate decision, not an in-session move. If you reach a point where you think a new library is needed, stop and propose it to the user with reasoning. Do not install it unilaterally.

The libraries explicitly banned in `stack-decision.md` ("What's explicitly out") are not options under any circumstance. If you find yourself reaching for 21st.dev, GSAP, Lottie, three.js, CSS-in-JS, or a state library — stop. They are out for documented reasons. (Sanity is the project CMS and is in-stack; other CMSs remain out.)

### Rule 4 — Read tokens via `tokens.ts`, not inline values.

The `scripts/build-tokens.js` script generates `lib/tokens.ts` from `tokens.json`. In component code, import from `tokens.ts`:

```tsx
import { tokens } from '@/lib/tokens';

<motion.div
  transition={{
    duration: parseInt(tokens.motion.duration.slow) / 1000,
    ease: tokens.motion.easing.out,
  }}
/>
```

For values that map to Tailwind classes (colours, spacing, radii, typography), use the Tailwind utility classes generated from the same tokens:

```tsx
<button className="bg-brand-navy text-on-brand rounded-button px-7 py-2.5">
  Talk to sales
</button>
```

Never use a raw hex code, raw pixel value, or raw motion duration in component code. If a token doesn't exist for what you need, stop and ask whether the token should be added — don't bypass the system.

### Rule 5 — Respect the third-person company voice rule.

The website addresses the buyer as "you" and refers to the company as "NymCard" by name. Never "we," "our," "us" referring to NymCard. This applies to button text, navigation, error messages, empty states, micro-copy — anywhere copy might be written outside `../02-copy/`. (The copy in `../02-copy/` is already correct; this rule covers UI strings you might author.)

This rule comes from the NymCard MCD §9.6 with a website-specific override allowing "you/your." Detailed in the copywriting skill (`../00-strategy/` if present, otherwise reference the NymCard MCD).

### Rule 6 — Motion respects `prefers-reduced-motion`.

Every motion in the codebase either:
- Wraps in a `useReducedMotion()` check from Framer Motion, or
- Uses Framer Motion's built-in respect for `prefers-reduced-motion` via the `MotionConfig` provider in the root layout.

Disabled motion categories: ambient, kinetic, cinematic. Preserved: responsive motion under 200ms (hover, focus). See `tokens.json` → `motion.accessibility`.

### Rule 7 — Server components by default. Client components only when needed.

Next.js App Router defaults to server components. Add `'use client'` only when a component genuinely needs client-side capability (interactivity, browser APIs, hooks like `useState` or `useEffect`, or Framer Motion). Pushing everything to client components negates the App Router's performance benefit.

A useful heuristic: if a component is static content + a Framer Motion wrapper, the wrapper is the client component and the content can be passed as children from a server component.

### Rule 8 — Accessibility is not optional.

Every interactive element must:
- Have a keyboard focus state (handled by `tokens.shadow.ring.default`).
- Be reachable via tab navigation.
- Have an accessible label (visible text or `aria-label`).
- Have a hit area of at least 44×44px on touch devices (CTAs already meet this with 10px 28px padding + button font size).

Use shadcn/ui primitives where they exist — they handle most accessibility correctly out of the box. When building bespoke marketing components, mirror the same patterns.

### Rule 9 — Build on the existing component and visual system.

Before building any page or section, audit what already exists in the repo — that body of components and visuals *is* the visual system:

- `components/sections/` — built marketing sections.
- `components/ui/` — shadcn primitives.
- `components/visuals/` — bespoke SVG visuals and illustrations.
- The other `components/` subfolders (`hero/`, `composition/`, etc.) — page-specific building blocks.

Default to reusing and composing what is already there. A new page or section should be assembled from existing parts wherever possible, so the site stays one coherent system.

When a task genuinely needs a section or visual the system does not yet cover — **stop before building it.** Flag it to the user first with a recommendation: what you propose to add, why the existing components and visuals don't cover the need, and how the new piece will stay consistent with `design-system.md` and the tokens. Never introduce an off-system section or visual unilaterally.

---

## Soft rules (judgement calls)

These are rules that almost always apply but have legitimate exceptions. Use judgement; when in doubt, ask.

- **Keep components in `components/sections/` focused.** A section component should render one section of one page. Don't compose multiple sections into a single component. The Hero is one component; the bento ProductsGrid is another.
- **Co-locate styles, not types.** Tailwind classes live inline in JSX. TypeScript types live in shared files (`lib/types.ts` or co-located `*.types.ts` when truly local).
- **Loading states for data-fetched content.** Any content fetched at runtime needs a sensible loading state — skeleton or layout shift prevention. Use shadcn's `Skeleton` primitive. (Marketing copy is not fetched — it's mirrored from `../02-copy/` — so this applies only to genuinely dynamic content.)
- **No `any` types in TypeScript.** If you need to escape the type system, use `unknown` and narrow. `any` is a code smell that almost always points to a missed type definition.
- **Tests are not required for the marketing site.** The site is mostly static content with motion. Component-level unit tests add little value here. If you find yourself wanting to write one, ask first — there may be a structural issue the test is masking.

---

## How to handle ambiguity

The previous build failed in part because Claude Code, when faced with ambiguity, invented an answer rather than asking. That is the failure mode this section exists to prevent.

When you encounter ambiguity in a handoff artefact, copy, or token:

1. **First, check if the answer is already in the source-of-truth documents** (`design-system.md`, `tokens.json`, `stack-decision.md`). Most of the time it is.
2. **If not, frame a specific question and ask the user.** Do not make a "best guess." A specific question that takes 30 seconds to answer is always better than a fix later.
3. **If the user is unavailable and you must proceed, mark the assumption clearly** in a code comment with `// ASSUMPTION:` prefix, and surface it in the PR description. The user can correct it during review.

Examples of good clarifying questions:
- "The handoff shows two buttons in the hero, but doesn't specify the secondary CTA copy. The current `../02-copy/Homepage.md` has 'Explore nCore' as secondary. Use that?"
- "The bento grid handoff shows the Card Issuing card in a 2x1 span. The token system supports this, but it changes the grid asymmetry from what's in the visual direction. Confirm this is intentional?"
- "The motion spec for the nCore module assembly is 'cinematic' but doesn't specify which elements assemble in what order. Sketch the order or shall I propose one?"

Examples of bad responses to ambiguity:
- Picking a hex code that's close to the design and shipping it.
- Picking an arbitrary section padding because "120px looks right."
- Writing your own copy because `../02-copy/` didn't have the string you needed.
- Installing a new library because "the design needs animation and Framer Motion doesn't do exactly this."

---

## Locked copy sources

The source of truth for each page's copy is a Markdown file in `../02-copy/`. The copy always lives there — components only mirror it.

| Page | Copy file |
| --- | --- |
| Homepage | `../02-copy/Homepage.md` |
| Navigation | `../02-copy/Navigation.md` |
| Card Issuing | `../02-copy/card-issuing-copy.md` |
| Lending | `../02-copy/Lending-copy.md` |
| Stablecoin Settlement | `../02-copy/stablecoin-setlement-copy.md` |
| (Other pages — to be added as their copy is locked) | |

When mirroring copy:
- Use exact strings. Do not edit punctuation, line breaks, or capitalisation.
- Mirror the copy verbatim into the component — a typed `COPY` const at the top of the section component is the established pattern (see `components/hero/Hero.tsx`).
- If `../02-copy/` and a component disagree, `../02-copy/` wins. Update the component, not the file.

---

## Folder structure

```
06-build/
├── app/                            ← Next.js App Router routes
│   ├── layout.tsx                  ← Root layout (Lenis init, MotionConfig, global providers)
│   ├── page.tsx                    ← Homepage (composed from components/sections/)
│   ├── platform/
│   │   ├── card-issuing/page.tsx
│   │   └── ...
│   └── ...
├── components/
│   ├── ui/                         ← shadcn copy-paste components (Button, Dialog, etc.)
│   ├── sections/                   ← Marketing sections (Hero, ProductsGrid, NCoreSection, etc.)
│   └── visuals/                    ← Bespoke SVG (HeroGradient, NCoreDiagram, KineticRibbon)
├── lib/
│   ├── tokens.ts                   ← Auto-generated from tokens.json (do not edit by hand)
│   ├── motion-tokens.ts            ← Helper for Framer Motion to read motion tokens
│   └── utils.ts                    ← Small utility functions (cn(), etc.)
├── public/
│   ├── icons/                      ← Bespoke SVG product icons
│   └── logos/                      ← Client logos
├── scripts/
│   └── build-tokens.js             ← Reads tokens.json, generates tokens.ts and tailwind config
├── tokens.json                     ← Source of truth (synced from ../01-design-system/)
├── tailwind.config.js              ← Generated (do not edit by hand)
├── next.config.js
├── tsconfig.json
├── package.json
└── CLAUDE.md                       ← This file
```

Rules about the folder structure:

- **Never edit `lib/tokens.ts` by hand.** It's generated. Run `npm run build:tokens` to regenerate.
- **Never edit `tailwind.config.js` by hand** for token-related values. The token-related sections are generated. If you need to add Tailwind plugins or non-token config, edit only the non-generated sections (clearly marked with comments).
- **Bespoke SVG visuals live in `components/visuals/`**, not `public/`. They're React components, not static assets.
- **Static assets (raster images, logos) live in `public/`** and are referenced via Next.js `<Image>`.

---

## Common tasks — how to handle them

### Building a new section

1. Audit the existing component and visual system first (`components/sections/`, `components/ui/`, `components/visuals/`, and the other `components/` subfolders) for parts to reuse. If the section needs something off-system, flag it to the user before building — see Rule 9.
2. Read the handoff artefact for the section (`../05-handoff/`).
3. Identify which `tokens.json` values apply: spacing, colours, radii, motion timings.
4. Identify which shadcn primitives apply (Button, etc.) and which bespoke components are needed.
5. Mirror the section copy from the relevant file in `../02-copy/`.
6. Build the section as a component in `components/sections/[SectionName].tsx`.
7. Wire motion using Framer Motion, reading durations and easings from `lib/tokens.ts`.
8. Test responsive behaviour at the breakpoints in `tokens.json` → `grid.breakpoints`.
9. Verify `prefers-reduced-motion` is respected.

### Adding a new shadcn component

```bash
npx shadcn-ui@latest add [component-name]
```

The component lands in `components/ui/`. Modify it to match `tokens.json` if shadcn's defaults don't align (most often: radius, colour palette).

### Adding a new icon

- System / UI icon (chevron, close, search, arrow): use Lucide.

```tsx
import { ArrowRight } from 'lucide-react';
```

- Brand / product icon (Card Issuing, Embedded Lending, etc.): bespoke SVG in `components/visuals/icons/` or `public/icons/` depending on whether it needs to be a React component or static asset.

### Adding a new page

1. Audit the existing component and visual system first (see Rule 9) — a new page should be composed from existing sections, components, and visuals wherever possible.
2. Create `app/[route]/page.tsx`.
3. Use the existing `RootLayout` for shared chrome (nav, footer).
4. Compose the page from section components. If the page needs a section or visual the system doesn't have, flag it to the user with a recommendation before building it.
5. Mirror copy from the relevant file in `../02-copy/` (add it to "Locked copy sources" in this file).
6. Update sitemap (`app/sitemap.ts`) if it exists.

### Updating a token

1. Edit `../01-design-system/tokens.json`.
2. Copy the updated file into the build folder root (or set up a sync script — preferred).
3. Run `npm run build:tokens` to regenerate `lib/tokens.ts` and the Tailwind config.
4. Test that affected components reflect the change.

Token updates are deliberate decisions. Do not update tokens to fit a one-off design need — the token system should not bend to a single component.

---

## What to do if something feels wrong

Three escalation levels:

**Level 1 — Internal check.** Re-read the relevant source-of-truth document. Most of the time, the answer is already there and you missed it.

**Level 2 — Frame a question.** Write the question as specifically as possible. Quote the conflict ("the handoff says X but `design-system.md` §8.5 says Y"). Ask the user.

**Level 3 — Stop and flag.** If you cannot proceed without breaking a rule in this document, stop. Do not make progress at the cost of fidelity. The user would rather wait for a clarifying answer than rebuild from a wrong assumption.

---

## What "good" looks like in this codebase

A successful contribution to this codebase produces code that:

- Looks visually identical to the handoff artefact at the breakpoints in `tokens.json`.
- Reads tokens via `lib/tokens.ts` and Tailwind utility classes only — no raw values in component code.
- Mirrors copy verbatim from `../02-copy/` — no invented marketing strings.
- Reuses the existing component and visual system; any genuinely new, off-system section or visual was flagged to the user first.
- Uses motion that maps to a section of `design-system.md` §9 — no improvised animations.
- Is accessible by default — keyboard navigable, screen-reader friendly, `prefers-reduced-motion` respecting.
- Adds zero new dependencies unless the user explicitly approved them.
- Is reviewable in a single PR — focused scope, clear commit messages, no incidental changes.

A failed contribution is one that ships but introduces drift — a hardcoded hex, a copy string that wasn't in `../02-copy/`, a library that wasn't in `stack-decision.md`. These don't fail visually at PR time; they fail months later when the team can't keep the site consistent.

---

## Document control

- **Version:** 1.6 (May 2026)
- **Owner:** Esha (VP Marketing) and the engineering effort
- **Authority:** Rules of engagement for Claude Code in this repository
- **Subordinate to:** `design-system.md`, `tokens.json`, `stack-decision.md`, `../02-copy/`, the NymCard MCD.

### Change log

- **v1.6 (May 2026):** Canonical glass kit locked as the product-UI standard, **superseding v1.4 note (2)**. Product-UI cells (homepage Products bento, product-page UI zones, industry-page UIs) are **bespoke coded surfaces composed on the glass kit** — `GlassSurface` (the §8.1 material, mirroring `GlassPanel`) floating on `GlassAtmosphere` (the rich, theme-aware, restrained cool field). See design-system.md §8.1 "Canonical implementation (v-glass)" and §8.8 (v6), and the "Design guardrails" section above. The v5 direction (every cell loading a reused `/public/handoff/*.svg` on a faint tonal bed via `HandoffVisual`) is retired as the *product-card* direction — it read flat (frost on a faint wash) and samey (reused hero assets). `HandoffVisual` now composes the kit internally so any surface still loading a handoff asset reads dimensional, but new product UIs are distinct coded surfaces, never reused SVGs. The procedural `CardsUI`/`LendingUI`/etc. that v1.4 called "retired" are in fact the **current** coded surfaces (rebuilt on the kit). Bar to match + verify against: **`/visual-system/glass`**. Navy/cyan-led, restrained, light-first; purple is an object accent only (never the field — the Paymentology trap).
- **v1.5 (May 2026):** No-eyebrow rule extended site-wide. Homepage AND inner-page section openers lead with the headline — the uppercase / mono-tracked `Eyebrow` scaffolding label is banned at the top of any marketing section. Per-card / per-cell `eyebrow` labels (a capability name, an industry name) stay — they are real content, not a scaffolding label.
- **v1.4 (May 2026):** Homepage redesign POV recorded (design-system.md v2.3). Three standing rules carried forward from that pass: (1) **No eyebrows on homepage section openers.** The uppercase / mono-tracked `Eyebrow` scaffolding label is banned at the top of homepage sections — lead with the headline. The `Eyebrow` atom still exists for older inner-page primitives, but new homepage work never opens a section with it; `RailCarousel.eyebrow` is optional for this reason. This enforces design-system.md §2 ("No all-caps anywhere. No eyebrow style."). (2) **Product-UI cells load real handoff surfaces, never procedural fakes.** Use `HandoffVisual` (`components/sections/product-uis/HandoffVisual.tsx`) to compose a Claude Design handoff SVG on a tonal bed; never paint a dashboard, ticker, particle field or window-chrome placeholder in TypeScript, and never invent data (numbers live in the SVG). The procedural `CardsUI`/`LendingUI`/etc. are retired from the homepage. (3) **Motion is mandatory and motivated.** The homepage must not read static: an orchestrated page-load on the hero, scroll-tied reveals on sections, and the kinetic ribbon recurring (hero → RibbonInterlude → CTASection). Every animation is `prefers-reduced-motion` safe and reads its durations/easings from `lib/visuals/motion` (Framer Motion only — GSAP/Lottie/three.js remain banned). Committed violet (`accent-violet` #6D28D9) is a real voice in hero/signature moments, not a ≤10% accent; CTAs still stay navy.
- **v1.0 (May 2026):** Initial document. Authored after `design-system.md` v1.2, `tokens.json` v1.2.0, and `stack-decision.md` v1.0 were locked.
- **v1.1 (May 2026):** Copy source of truth moved to the `../02-copy/` Markdown files. All Notion references removed — copy is mirrored from `../02-copy/`, not fetched.
- **v1.2 (May 2026):** Added Rule 9 — audit and build on the existing component and visual system before building any page or section; flag any genuinely new, off-system section or visual to the user first.
- **v1.3 (May 2026):** Sanity adopted as the project CMS (see stack-decision.md v1.2). The blanket "no CMS" constraint is lifted — Sanity is in-stack; other CMSs remain out.

### Open decisions

- Whether to add automated linting that enforces the hard rules (no raw hex codes, no hardcoded marketing strings). Default: not now, add later if drift recurs.
- Whether to add a `pre-commit` hook that runs `npm run build:tokens` to keep `lib/tokens.ts` in sync. Default: yes, low-cost, prevents stale tokens.
- Whether `CLAUDE.md` itself should be versioned in lockstep with the source-of-truth documents, or evolved independently. Default: independently, with a change log entry whenever it updates.
</content>
